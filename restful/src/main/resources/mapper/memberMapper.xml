<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.restful.mapper.MemberMapper">
    <insert id="insert" parameterType="MemberVO">
        <selectKey resultType="Long" keyProperty="id" order="BEFORE">
            SELECT SEQ_MEMBER.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_MEMBER
        VALUES (SEQ_MEMBER.NEXTVAL, #{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <select id="existByMemberEmail" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

    <select id="selectByMemberEmailAndMemberPassword" parameterType="MemberVO" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
          AND MEMBER_PASSWORD = #{memberPassword}
    </select>

    <select id="existByMemberIdAndMemberEmailForUpdate" parameterType="MemberVO" resultType="int">
        SELECT COUNT(*)
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
          AND ID != #{id}
    </select>

    <select id="select" parameterType="Long" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
        WHERE ID = #{id}
    </select>

    <select id="selectAll" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
    </select>

    <update id="update" parameterType="MemberVO">
        UPDATE TBL_MEMBER
        <set>
            <if test="memberName != null and memberName != ''">
                MEMBER_NAME = #{memberName}
            </if>
            <if test="memberPassword != null and memberPassword != ''">
                MEMBER_PASSWORD = #{memberPassword}
            </if>
        </set>
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_MEMBER
        WHERE ID = #{id}
    </delete>
</mapper>